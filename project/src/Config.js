// API 路徑
// 上傳、讀取圖片的路徑個別設定

const host = "http://localhost:3500";

const config = {
  AB_List: `${host}/address-book/api/list`,
  MYFORM_API: `${host}/uploadFromReact/myform`,
  IMG_PATH:`${host}/img`,
};

export default config;
